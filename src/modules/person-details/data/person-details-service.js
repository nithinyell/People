// Person Details Api

/*
const fetchPersonDetails = async(personId) => {
    try {
        let response = await fetchPersonDetails(personId);
        return response
    } catch(error) {
        // handle error
    }
};
*/